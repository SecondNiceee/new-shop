class Router{
    profile = "/profile"
    home = "/"
    checkout = "/checkout"
    orders = "/orders"
    favorited = "/favorites"
    mobileLogin = "/login"
    mobileCart = "/mobile-cart"
    mobileCatalog = "/catalog"
    product = "/product"
    blog = "/blog"

}
export const routerConfig = new Router();